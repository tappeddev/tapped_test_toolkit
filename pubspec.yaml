name: tapped_test_toolkit
description: A new Flutter project.
version: 0.0.1
publish_to: none

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"


dependencies:
  args: ^2.4.0
  http: ^0.13.5
  freezed_annotation: ^2.2.0
  json_annotation: ^4.8.0
  path: ^1.8.3
  # formatting the time of some jobs
  intl: ^0.17.0

dev_dependencies:
  build_runner: ^2.3.3
  flutter_lints: ^2.0.1
  freezed: ^2.3.2
  tapped_lints:
    git:
      url: https://github.com/tappeddev/tapped_lints.git
      ref: master

flutter:
